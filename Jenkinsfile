pipeline {
    agent any

    stages {
        stage('run') {
            steps {
                script {
                    // Use 'sh' step to execute shell commands
                    sh 'pwd'
                }
            }
        }
    }
}
